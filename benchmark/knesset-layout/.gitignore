# Locally generated by prepare-annotation.ts (regenerable from Object Storage) — not committed.
pdfs/
drafts/
# Local safety backups (timestamped hash manifests) — regenerable, not the freeze.
checkpoints/
# Human ground truth IS committed (the benchmark's value): ground-truth/*.json
# Append-only per-page audit trail IS committed: ground-truth/history/*.jsonl
# Deterministic workload triage report (regenerable): triage/
triage/
